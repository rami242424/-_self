<!DOCTYPE html>
<html lang="ko">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>연습문제 1</title>
</head>
<style>
	body {
			font-size:1.2em;
			text-align:center;
		}
</style>
<body>
	<h2>4의 배수일까 아닐까</h2>

	<script>

		// 내가 푼것
	 	// var num = parseInt(prompt("숫자를 입력하세요."));

		// function confirm(x){
		// 		if ( x % 4 === 0) {
		// 			document.write(x + "는 4의 배수입니다.");
		// 		} else {
		// 			document.write(x + "는 4의 배수가 아닙니다.");
		// 		}
		// }
		// confirm(num);

		//교안답
		// var userNumber = parseInt(prompt("숫자를 입력하시오."));

		// if (userNumber !== null) {
		// 	if (userNumber % 4 === 0)
		// 		document.write(userNumber +"는 4의 배수입니다.");
		// 	else
		// 		document.write(userNumber +"는 4의 배수가 아닙니다.");
		// }

	</script>
</body>
</html>
