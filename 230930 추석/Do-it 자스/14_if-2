<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>조건문</title>
</head>
<body>
    <script>

      // if ~ else 안에 if ~ else 중첩
      var userNumber = prompt('숫자를 입력하세요.');

      if (userNumber !== null) {
        if (userNumber % 3 === 0)
          alert("3의 배수입니다.");
        else
          alert("3의 배수가 아닙니다.");
        } 
        
      else
        alert("입력이 취소됐습니다."); //취소 누를때 알럿

    </script>
</body>
</html>
