<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>조건문</title>
</head>
<body>
    <script>  
        // 내가한것: 이렇게만 입력해도 '취소'시 입력이 취소되었습니다 로 자동 출력되네??!!

        var num = prompt("숫자를 입력하세요.");

        num % 3 === 0 ? alert('3의 배수입니다.') : alert('3의 배수가 아닙니다.')

        // 교재

        // var num = prompt('숫자를 입력하세요.');

        // if (num !== null)
        //   null % 3 === 0 ? alert('3의 배수입니다.') : alert('3의 배수가 아닙니다.')
        // else
        //   alert('입력이 취소되었습니다.');    
    </script>
</body>
</html>
